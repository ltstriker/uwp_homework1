<Page x:ConnectionId='1' x:Name="page"
    x:Class="App1.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local1="using:App1.Converters"
      xmlns:md="using:App1.Models"
      mc:Ignorable="d" Height="Auto" Width="Auto" Background="#FFF9F3F3">

    <Page.Resources>
        <ResourceDictionary>
            <local1:MyConverter x:Key="MyConverter"/>
            <local1:MyConverter2 x:Key="MyConverter2"/>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='2' x:Name="VisualStateGroup">
                <VisualState x:ConnectionId='3' x:Name="min0">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="right.(UIElement.Visibility)" Value="Collapsed"/>
                        <Setter Target="state0.(UIElement.Visibility)" Value="Collapsed"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:ConnectionId='4' x:Name="min600">
                    <VisualState.Setters>
                        <Setter Target="right.(UIElement.Visibility)" Value="Collapsed"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:ConnectionId='5' x:Name="min800">
                    <VisualState.Setters>
                        <Setter Target="page.(UIElement.Visibility)" Value="Visible"/>
                    </VisualState.Setters>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800"/>
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="0">
            <ListView x:ConnectionId='8' IsItemClickEnabled="True"                                                                          >
                <ListView.ItemTemplate>
                    <DataTemplate                         >
                        <UserControl x:ConnectionId='9'>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="42"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox x:ConnectionId='10' Name="checkBox" Grid.Column="0" Content="" FontSize="20"                                              />
                                <Image Grid.Column="1" Width="100" Height="100" Source="{Binding Image}" Visibility="{Binding Visibility, ElementName=state0}"/>
                                <TextBlock x:ConnectionId='11' Grid.Column="2"                                    HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <Line Stretch="Fill" Stroke="Black" VerticalAlignment="Center" Grid.Column="2" StrokeThickness="2" X1="1" Visibility="{Binding IsChecked, ElementName=checkBox}"/>
                            </Grid>
                        </UserControl>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        <Grid x:ConnectionId='6' x:Name="state0"></Grid>
        <Frame x:ConnectionId='7' x:Name="right" Grid.Column="1"></Frame>
    </Grid>


    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:ConnectionId='12' Icon="Add" Label="Add"                    Width="Auto"                                                                                             ></AppBarButton>
            <AppBarButton x:ConnectionId='13' x:Name="delete" Icon="Delete" Label="Delete"                Width="Auto"                                                                                            ></AppBarButton>
            <AppBarButton x:ConnectionId='14' x:Name="Share" Icon="Send" Label="Share"                     Width="Auto"                                                                                            ></AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>
</Page>

